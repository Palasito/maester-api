# Managed dependencies disabled — modules are self-installed by profile.ps1
# on first cold-start to /home/site/modules/ (persistent Azure Files storage).
# See profile.ps1 for the installation logic and version pins.
@{}
